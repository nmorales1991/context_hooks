import React from 'react'

export default React.createContext()//acá se crea el contexto, se puede dejar un estado por defecto pero solo será usado cuando el componente no está englobado en un provider